"""foldersync — compare and manually sync files between two folders.

Each folder can be local or remote (SSH/SFTP). Two compare modes:
Normal (recursive walk) and Git (narrow scope via git repo info).
"""

__version__ = "0.1.0"
